RSpec.describe HustleSupport::CoreExt do
  it "has a version number" do
    expect(HustleSupport::CoreExt::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
